using Raylib_cs;
using System.Numerics;
class Collisions : Score
{
    // public bool touchCheckGem(Gems gem, Character character)
    // {
    //     var posStart1 = gem.position;
    //     var posEnd1 = gem.position + gem.velocity;
    //     var posStart2 = character.position;
    //     var posEnd2 = character.position + character.velocity;
    //     var avgX = (gem.position.X + character.position.X) / 2;
    //     var avgY = (gem.position.Y + character.position.Y) / 2;
    //     var collPoint = new Vector2(avgX, avgY);
    //     // return Raylib.CheckCollisionLines(posStart1, posEnd1, posStart2, posEnd2, collPoint);
    // }
    // public bool touchCheckRocks(Rocks gem, Character character)
    // {
    //     var posStart1 = gem.position;
    //     var posEnd1 = gem.position + gem.velocity;
    //     var posStart2 = character.position;
    //     var posEnd2 = character.position + character.velocity;
    //     var avgX = (gem.position.X + character.position.X) / 2;
    //     var avgY = (gem.position.Y + character.position.Y) / 2;
    //     var collPoint = new Vector2(avgX, avgY);
    //     // return Raylib.CheckCollisionLines(posStart1, posEnd1, posStart2, posEnd2, collPoint);
    // }
}
